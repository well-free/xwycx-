<view class="page surface"><view class="section-title">全部商品</view><view wx:if="{{loading}}" class="loading">加载中...</view><product-card wx:for="{{items}}" wx:key="id" item="{{item}}" binddetail="detail" bindadd="add" /></view>
