worker_processes 1;

events {
  worker_connections 1024;
}

http {
  include mime.types;
  default_type application/octet-stream;
  sendfile on;
  keepalive_timeout 65;
  access_log /var/log/nginx/xwycx.access.log;
  error_log /var/log/nginx/xwycx.error.log warn;

  upstream xwycx_app {
    server 127.0.0.1:8080;
    keepalive 32;
  }

  server {
    listen 80;
    server_name xwycx.xyz www.xwycx.xyz;
    return 301 https://$host$request_uri;
  }

  server {
    listen 443 ssl http2;
    server_name xwycx.xyz www.xwycx.xyz;

    ssl_certificate /etc/letsencrypt/live/xwycx.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/xwycx.xyz/privkey.pem;
    ssl_session_timeout 1d;
    ssl_session_cache shared:xwycx_ssl:10m;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
      proxy_pass http://xwycx_app;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /api/ {
      proxy_pass http://xwycx_app;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }
}
